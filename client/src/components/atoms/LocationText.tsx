export function LocationText({ children }: { children: React.ReactNode }) {
  return <span className='text-slate-600'>{children}</span>;
}
// LocationText: Styled text element emphasizing location/neighborhood info.
